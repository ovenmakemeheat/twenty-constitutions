pre-commit:
  jobs:
    - name: prettier
      glob: "*.{ts,tsx,js,jsx,mjs,cjs,json,md,mdx,css,scss,html,yml,yaml}"
      run: bunx prettier --write --ignore-unknown {staged_files}
      stage_fixed: true

commit-msg:
  jobs:
    - name: commitlint
      run: bunx commitlint --edit {1}
